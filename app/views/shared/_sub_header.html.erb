<header class="relative bg-primary text-primary-content h-14">
  <div class="h-full px-4 flex items-center justify-between">

    <% safe_back_url = url_from(request.referer) || home_path %>
    <%= link_to safe_back_url, class: "btn btn-ghost btn-circle" do %>
      ＜
    <% end %>

    <h1 class="text-lg font-bold">
      <%= title %>
    </h1>

    <div class="w-10"></div>

  </div>
</header>